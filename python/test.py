import random

def f(x):
    return random.randrange(1,x)

for i in range(2,10):
    print f(i)
   
if _name_ == '_main_':
    hello

    class myclass(object):
        """Docume myclass(args)class
        """
        def _init_(self, args):
            super(myclass, self)._init_()
            self.args = args
